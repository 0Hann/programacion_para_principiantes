
export let empleados: string[] = ['Fernando', 'Melisa', 'Johan']
export let salarios: number[] = [1500, 2400, 3200]

// let flores: string[] = ['Rosa','Girasol','Lirio']

// for(let i = 0; i < flores.length ; i++ ) {
//     console.log(flores[i]);
// }

// for(let i = 0; i < empleados.length; i++ ) {
//     console.log(empleados[i] + ' tiene un salario de: ' + salarios[i]);
// }
